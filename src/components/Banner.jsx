import './styling.css';

function Banner(){
   return(
      <><header className="header">Orbit Report</header>
      <p>Click on the buttons to see the satellites in that orbit type</p></>
   );
}

export default Banner;
